{
  "*.{js,svelte}": "eslint --ignore-path .gitignore --fix --cache",
  "*.scss": "stylelint --ignore-path .gitignore --fix",
  "*.{js,svelte,css,scss,html,json,md}": "prettier --ignore-path .gitignore --write"
}
